# spotifyYoutubeDownloader
downloads the contents of a spotify playlist from youtube videos

# Dependencies
  python  
  beautifulsoup:  
      `pip install bs4`  
  youtube-dl  
      `pip install youtube-dl` 

# Usage
  `python downloadSpotifyPlaylist.py spotifyPlaylistUri`
